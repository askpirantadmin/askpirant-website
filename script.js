const menuBtn=document.querySelector(".menu-btn"),nav=document.querySelector(".nav-links");if(menuBtn){menuBtn.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuBtn.setAttribute("aria-expanded",open)});document.querySelectorAll(".nav-links a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");menuBtn.setAttribute("aria-expanded","false")}))}document.querySelectorAll(".accordion details").forEach(d=>d.addEventListener("toggle",()=>{if(d.open)document.querySelectorAll(".accordion details").forEach(x=>{if(x!==d)x.open=false})}));const cards=[...document.querySelectorAll(".review-card")],dots=[...document.querySelectorAll(".dot")];let reviewIndex=0;function showReview(i){reviewIndex=(i+cards.length)%cards.length;cards.forEach((c,n)=>c.classList.toggle("active",n===reviewIndex));dots.forEach((d,n)=>d.classList.toggle("active",n===reviewIndex))}document.querySelector(".prev")?.addEventListener("click",()=>showReview(reviewIndex-1));document.querySelector(".next")?.addEventListener("click",()=>showReview(reviewIndex+1));dots.forEach((d,n)=>d.addEventListener("click",()=>showReview(n)));setInterval(()=>showReview(reviewIndex+1),6500);document.getElementById("year").textContent=new Date().getFullYear();